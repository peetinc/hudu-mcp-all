# Hudu base URL — example: https://yourorg.huducloud.com/api/v1
HUDU_BASE_URL=https://yourorg.huducloud.com/api/v1

# API key — get from Hudu Admin > API Keys
# Provide EITHER HUDU_API_KEY (inline) OR HUDU_API_KEY_FILE (path to file containing the key).
# No default file path. One of the two must be set explicitly. HUDU_API_KEY wins if both set.
HUDU_API_KEY=

# Optional: read API key from a file. Tilde (~) expanded. No default path.
# Example: HUDU_API_KEY_FILE=~/.config/hudu/api-key
HUDU_API_KEY_FILE=

# Optional: override bundled swagger.json
HUDU_SWAGGER_PATH=

# Optional: comma-separated operationIds to disable (e.g. "delete_companies_id,delete_articles_id")
HUDU_DISABLED_OPERATIONS=

# Optional: block all DELETE operations
HUDU_READONLY=false

# Optional: request timeout in ms (default 30000)
HUDU_TIMEOUT_MS=30000

# Optional: max retries on HTTP 429 (default 3). Respects Retry-After header.
HUDU_MAX_RETRIES=3

# Optional: max response body size in bytes; larger responses are truncated to fit MCP transport limits (default 100000 = ~100KB)
HUDU_MAX_RESPONSE_BYTES=100000

# Optional: preset to narrow the tool surface. Valid: all, readonly, core, kb, assets, passwords, ipam, processes, admin
# Default: all (149 tools). See README for tag composition of each preset.
HUDU_PRESET=

# Optional: whitelist Swagger tags (comma-separated, case-insensitive). Intersects with preset.
# Example: HUDU_ENABLE_TAGS=Companies,Articles,IP Addresses
HUDU_ENABLE_TAGS=

# Optional: blacklist Swagger tags (comma-separated, case-insensitive). Subtracts after preset/enable.
# Example: HUDU_DISABLE_TAGS=Rack Storage Items,Magic Dash
HUDU_DISABLE_TAGS=
